env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = GDK_BACKEND,wayland,x11
env = QT_QPA_PLATFORM,wayland;xcb
env = QT_QPA_PLATFORMTHEME,qt5ct
env = QT_STYLE_OVERRIDE,kvantum

# Exec Block
exec-once = waybar
exec-once = alacritty
exec-once = sh -c "sleep 2; ~/.config/hypr/scripts/hyprpaper-all.sh"
exec-once = hyprsunset
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
exec-once = ~/.config/hypr/scripts/ws-history.sh daemon

# Mod key
$mod = SUPER

# Launcher + apps
bind = $mod, RETURN, exec, alacritty
bind = $mod, B, exec, firefox
bind = $mod, F, exec, dolphin
bind = $mod, S, exec, wofi --show drun
bind = $mod, P, exec, ~/.config/wofi/powermenu.sh
bind = $mod, Tab, exec, ~/.config/hypr/scripts/app-switcher.sh

# Clipboard Manager
bind = $mod SHIFT, V, exec, /home/nlion/.config/hypr/scripts/cliphist-wofi-img.sh | wl-copy

# Window handling
bind = $mod, Q, killactive
bind = $mod, V, togglefloating

bind = $mod, H, movefocus, l
bind = $mod, L, movefocus, r
bind = $mod, K, movefocus, u
bind = $mod, J, movefocus, d

bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

# Resize mode
bind = $mod, R, submap, resize
submap = resize
binde = , right, resizeactive, 30 0
binde = , left, resizeactive, -30 0
binde = , up, resizeactive, 0 -30
binde = , down, resizeactive, 0 30
bind = , escape, submap, reset
submap = reset

# Workspaces 1-10
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

# Move active window to workspace 1-10
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10

# Global workspace navigation
bind = $mod, mouse_down, workspace, r+1
bind = $mod, mouse_up, workspace, r-1
bind = $mod, right, workspace, r+1
bind = $mod, left, workspace, r-1

# Local workspace navigation on the current monitor
bind = $mod SHIFT, right, workspace, m+1
bind = $mod SHIFT, left, workspace, m-1

# Move Windows
bind = $mod SHIFT, H, movewindow, l
bind = $mod SHIFT, J, movewindow, d
bind = $mod SHIFT, K, movewindow, u
bind = $mod SHIFT, L, movewindow, r

# Jump to next empty workspace on the current monitor
bind = $mod, N, workspace, emptynm

# Send active window to next empty workspace on the current monitor
bind = $mod SHIFT, N, movetoworkspace, emptynm

# Jump to previous workspace
bind = $mod, M, exec, ~/.config/hypr/scripts/ws-history.sh back

# Jump to previous workspace and follow it
bind = $mod SHIFT, M, exec, ~/.config/hypr/scripts/ws-history.sh move-back-follow

# Fix play/pause media keys
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
bindl = , XF86AudioNext, exec, playerctl next

# German Keyboard
input {
    kb_layout = de

    touchpad {
        natural_scroll = yes
    }

    follow_mouse = 2
    mouse_refocus = false
}

misc {
    mouse_move_focuses_monitor = false
}

general {
    gaps_in = 2
    gaps_out = 3
    border_size = 2
    col.active_border = rgba(cba6f7ee) rgba(8b5cf6ee) 45deg
    col.inactive_border = rgba(7c7f93aa)
}

decoration {
    rounding = 10
    active_opacity = 1.0
    inactive_opacity = 0.96
    dim_inactive = true
    dim_strength = 0.04

    shadow {
        enabled = true
        range = 12
        render_power = 3
        color = rgba(1a1028aa)
    }
}

# Opacity window rules
windowrule = opacity 0.96 override 0.86 override, match:class ^(Alacritty)$
windowrule = opacity 1 override 0.92 override, match:class ^(spotify)$
windowrule = opacity 1 override 0.92 override, match:class ^(elecwhat)$

# Fix for scaling in games
xwayland {
    force_zero_scaling = true
}

binds {
  allow_workspace_cycles = true
}

# Monitor Setup
# Laptop:
# monitor = eDP-1, 1920x1080@60.02, auto, 1
# Desktop: (future)
monitor = DP-3, 1920x1080@144, 0x0, 1
monitor = DP-1, 2560x1440@165, 1920x0, 1.25
monitor = DP-2, 1920x1080@144, 3968x0, 1
